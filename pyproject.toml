[tool.poetry]
name = "geodeticconvert"
version = "0.1.0"
description = ""
authors = ["roywaswa <roywaswa.rw@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
pytest = "^8.0.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
